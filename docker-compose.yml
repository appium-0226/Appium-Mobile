services:

  # db:
  #   image: mysql:9.2.0
  #   platform: linux/amd64
  #   container_name: mysql-db
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=mhasan123
  #     - MYSQL_DATABASE=simpleandroid_db
  #   ports:
  #     - "3307:3306"

  test-runner:
    build: .
    container_name: appium-test-runner
    # depends_on:
    #   - db
    environment:
      # - db.url=jdbc:mysql://db:3306/simpleandroid_db
      - db.url=${DB_URL}
      - db.user=${DB_USER}
      - db.password=${DB_PASSWORD}
      - appiumUrl=${APPIUM_URL}
      - HOST_PWD=${PWD}
    volumes:
      - ./target:/app/target
      - maven-cache:/root/.m2
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  maven-cache:
