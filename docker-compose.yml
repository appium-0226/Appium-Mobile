services:

  # db:
  #   image: mysql:9.2.0
  #   platform: linux/amd64
  #   container_name: mysql-db
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=mhasan123
  #     - MYSQL_DATABASE=simpleandroid_db
  #   ports:
  #     - "3307:3306"

  test-runner:
    build: .
    container_name: appium-test-runner
    # depends_on:
    #   - db
    environment:
      # - db.url=jdbc:mysql://db:3306/simpleandroid_db
      - db.url=jdbc:mysql://150.109.22.19:3306/simpleandroid_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      - db.user=mhasan
      - db.password=mhasan123
      - appiumUrl=http://host.docker.internal:4723
      - HOST_PWD=${PWD}
    volumes:
      - ./target:/app/target
